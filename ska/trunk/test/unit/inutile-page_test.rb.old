require File.dirname(__FILE__) + '/../test_helper'


class PageTest < Test::Unit::TestCase
  fixtures :pages

  def setup
    @page = Page.find(1)
  end

  # Replace this with your real tests.
  def test_truth
    assert_kind_of Page,  @page
  end
  def test_pagename
    assert_equal Page.new('hello'), File.join(RAILS_ROOT,'hello.text')
  end
    
end
